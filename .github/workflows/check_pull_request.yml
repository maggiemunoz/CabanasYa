name: Linters
on: pull_request
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Install dependencies 📦
        run: bundle install

      - name: Run Rubocop
        run: rubocop 